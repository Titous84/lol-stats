// Téléchargement Data Dragon complet livré en L3 (docs/TASKS.md § L3).
import { config } from "dotenv";

config({ path: ".env.local" });

console.log("sync-assets : livré en L3 (docs/TASKS.md § L3). Rien à faire pour l'instant.");
